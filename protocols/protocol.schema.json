{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Agent Protocol Definition",
  "description": "A schema for defining a single, modular agent protocol.",
  "type": "object",
  "properties": {
    "protocol_id": {
      "description": "A unique, machine-readable identifier for the protocol (e.g., 'core-directive-001').",
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "description": {
      "description": "A human-readable summary of the protocol's purpose.",
      "type": "string"
    },
    "schema_version": {
      "description": "The version of the AGENTS.md schema this protocol adheres to.",
      "type": "string"
    },
    "agent_id": {
      "description": "The designated name of the agent this file is primarily for.",
      "type": "string"
    },
    "core_mandate": {
      "description": "The high-level objective and purpose of the agent within this repository.",
      "type": "string"
    },
    "knowledge_artifacts": {
      "description": "Pointers to key machine-readable files that provide the agent with deep contextual understanding.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {"type": "string"},
          "path": {"type": "string"},
          "description": {"type": "string"}
        },
        "required": ["name", "path", "description"]
      }
    },
    "permissions": {
      "description": "Explicit lists of allowed and prohibited actions to establish clear operational guardrails.",
      "type": "object",
      "properties": {
        "allowed_actions": {"type": "array", "items": {"type": "string"}},
        "prohibited_actions": {"type": "array", "items": {"type": "string"}}
      },
      "required": ["allowed_actions", "prohibited_actions"]
    },
    "rules": {
      "description": "An array of formal rules that constitute the protocol.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "rule_id": {
            "description": "A unique identifier for the rule within the protocol.",
            "type": "string"
          },
          "description": {
            "description": "A detailed, formal description of the rule to be followed.",
            "type": "string"
          },
          "enforcement": {
            "description": "Describes how the rule is programmatically enforced.",
            "type": "string"
          }
        },
        "required": ["rule_id", "description", "enforcement"]
      }
    },
    "associated_tools": {
      "description": "An optional list of tools that are directly related to or governed by this protocol.",
      "type": "array",
      "items": {
        "type": "string",
        "description": "The name of the tool or script (e.g., 'tooling/fdc_cli.py')."
      }
    }
  },
  "required": ["protocol_id", "description"]
}