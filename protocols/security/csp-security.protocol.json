{
  "protocol_id": "csp-security-protocol",
  "extends": "./base-security.protocol.json",
  "description": "A specific protocol for Content Security Policy (CSP).",
  "rules": [
    {
      "rule_id": "general-security-guideline",
      "description": "All web content must have a strict Content Security Policy.",
      "enforcement": "CSP headers must be configured on the web server."
    },
    {
      "rule_id": "csp-reporting-directive",
      "description": "A 'report-uri' directive must be included in the CSP to log violations.",
      "enforcement": "Automated header scanning in CI/CD pipeline."
    }
  ]
}