// integration_demo.aura

// 1. Call the HDL prover with a provable sequent
let result1 = agent.call_tool("hdl_prover", "A |- A");
print(result1);

// 2. Call the HDL prover with another provable sequent
let result2 = agent.call_tool("hdl_prover", "A, A -> B |- B");
print(result2);

// 3. Call the message_user tool
agent.call_tool("message_user", "Integration demo complete!");
