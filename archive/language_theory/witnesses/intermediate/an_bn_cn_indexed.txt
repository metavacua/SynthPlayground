# Corrected Indexed Grammar for L = {a^n b^n c^n | n >= 1}
# Symbols are separated by spaces.
#
# S -> T [f]
# T -> T [f] | A B C
#
# A[f] -> a A
# B[f] -> b B
# C[f] -> c C
#
# A[] ->
# B[] ->
# C[] ->

S -> T [f]
T -> T [f] | A B C

A[f] -> a A
B[f] -> b B
C[f] -> c C

A[] ->
B[] ->
C[] ->